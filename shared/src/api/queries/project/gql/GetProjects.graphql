query GetProjects($first: Int, $after: String) {
  projects(first: $first, after: $after, includeSkeleton: true) {
    edges {
      cursor
      node {
        ...ProjectFragment
      }
    }
    pageInfo {
      endCursor
      hasNextPage
      hasPreviousPage
      startCursor
    }
  }
}
