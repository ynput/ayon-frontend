query GetActivityUsers($projects: [String!]) {
  users(last: 5000, projects: $projects, isSupport: false) {
    edges {
      node {
        name
        attrib {
          fullName
        }
      }
    }
  }
}
