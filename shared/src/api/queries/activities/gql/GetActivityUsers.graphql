query GetActivityUsers($projects: [String!]) {
  users(last: 2000, projects: $projects, isSupport: false) {
    edges {
      node {
        name
        attrib {
          fullName
        }
      }
    }
  }
}
