query GetVersionsAttribs(
  $projectName: String!
  $versionIds: [String!]!
  $versionAttribNames: [String!]
) {
  project(name: $projectName) {
    versions(ids: $versionIds) {
      edges {
        node {
          id
          attrib(names: $versionAttribNames)
        }
      }
    }
  }
}
