query GetKanbanProjectUsers($projects: [String!]) {
  users(last: 5000, projects: $projects, isSupport: false) {
    edges {
      node {
        name
        accessGroups
        isManager
        isAdmin
        attrib {
          fullName
        }
      }
    }
  }
}
