import { FC } from 'react'
import * as Styled from './LinksManager.styled'

export interface LinksManagerProps {}

export const LinksManager: FC<LinksManagerProps> = ({}) => {
  return <Styled.Container>LinksManager</Styled.Container>
}
