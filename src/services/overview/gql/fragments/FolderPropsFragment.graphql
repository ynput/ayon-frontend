fragment FolderPropsFragment on FolderNode {
  id
  projectName
  parentId
  label
  name
  ownAttrib
  status
  tags
  folderType
  type
  updatedAt
  active
  hasReviewables
  createdAt
  attrib {
    ...FolderAttributesFragment
  }
}
